[project]
name = "mahmah-harness"
version = "0.0.0"
description = "riichienv diff harness for mahmah (not part of the Zig server)"
requires-python = ">=3.11"
dependencies = [
    "riichienv>=0.1.0",
    "pyyaml>=6.0",
]

[tool.uv]
package = false

[project]
name = "mahmah-tests"
version = "0.0.0"
description = "Python tooling for mahmah engine diffs against riichienv"
requires-python = ">=3.11"
dependencies = [
    "riichienv>=0.1.0",
    "pyyaml>=6.0",
]

[tool.uv]
package = false

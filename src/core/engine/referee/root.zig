//! 裁判：牌形判定、面子分解与得点结算。
pub const score = @import("score.zig");
pub const shape = @import("shape.zig");
pub const decomp = @import("decomp.zig");

test {
    _ = score;
    _ = shape;
    _ = decomp;
}
